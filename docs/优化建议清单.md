# 优化建议清单

基于在线心率算法集成后的代码审查，按优先级列出需要优化或根据调试结果进一步改进的部分。

生成日期: 2026-03-28
最后更新: 2026-03-30 (第二轮优化)

---

## 一、需要硬件调试后决定的问题

### ~~P0 - 运动阈值校准~~ ✅ 已完成 (2026-03-28)

- **修改前**: `Default_Motion_Th = 200.0` (量纲不明确)
- **修改后**: 添加 `HR_ACC_LSB_PER_G = 8192.0f` 常量，阈值改为 g 值单位，默认 `0.07g`（基于实测静息 0.06-0.07g）
- **位置**: `hr_algorithm.h` 添加 ACC 灵敏度常量, `hr_algorithm.c` 中阈值自动转换为 LSB
- **效果**: 配置更直观，阈值与实际物理量对应

### ~~P0 - BPF 系数验证~~ ✅ 已完成 (2026-03-28)

- **修改前**: 2 个 biquad 节，系数错误（截止频率 0.999Hz/4.001Hz，通带衰减 98dB）
- **修改后**: 4 个 biquad 节，使用 MATLAB butter() 生成的正确系数
- **验证结果**: -3dB 截止频率 0.500Hz / 5.000Hz，通带衰减 4.35dB，误差 < 0.1%
- **影响文件**: `hr_dsp.h` (系数数组 10→20), `hr_algorithm.h` (状态数组 8→16), `hr_dsp.c` (系数更新)

### ~~P1 - LMS 收敛稳定性~~ ✅ 已完成 (2026-03-29)

- **修改前**: 每秒调用 `LMS_Init` 重置 LMS 系数，滤波器永远无法充分收敛
- **修改后**: 仅在首次运行 (`win_count == HR_WIN_SEC`) 或 LMS 阶数变化时才重置系数，否则保留系数继续自适应
- **位置**: `hr_algorithm.c` Step 7, `hr_algorithm.h` `HR_State_t` 增加 `prev_order_hf`/`prev_order_acc`
- **效果**: LMS 系数跨窗口保持连续，收敛后滤波效果显著提升

### ~~P1 - PPG 信号范围~~ ✅ 已确认 (2026-03-28)

- **数据解算**: 已确认正确。18-bit ADC 精度完整保留（`& 0x03FFFF`），算法输入 float 均值计算正确。
- **实测范围**: PPG 均值 500-2000（相对值），符合正常佩戴下的信号强度。
- **LED 配置**: 当前 `LED3_PA = 0x71`，如需增强信号可调至 `0xFF`（最大电流）。
- **数据精度**: 算法内部使用 float 保持完整精度，无信息丢失。

### P2 - HF 参考信号有效性

- **现状**: LMS-HF 路径使用 ADS124 的 HF 通道作为运动伪影参考
- **位置**: `hr_algorithm.c:430-504` (Step 8)
- **建议**: 实测验证 HF 信号是否真的包含与 PPG 运动伪影相关的信息。如果相关性低，这条路径可以直接砍掉，节省约 30% 算力和 3KB RAM
- **验证方法**: 对比 LMS-HF 与 LMS-ACC 的 BPM 输出一致性

---

## 二、代码层面可直接优化

### ~~P0 - LMS 系数每秒重置 (最大算法缺陷)~~ ✅ 已完成 (2026-03-29)

- **修改前**: `LMS_Init` 每秒调用一次，所有系数清零，LMS 永远无法充分收敛
- **修改后**: 仅在首次运行或 LMS 阶数变化时才重置，保留系数跨窗口连续自适应
- **位置**: `hr_algorithm.c` Step 7, `hr_algorithm.h` 增加 `prev_order_hf`/`prev_order_acc`

### ~~P1 - 重复 FFT 计算~~ ✅ 已完成 (2026-03-30)

- **修改前**: 三条路径中频谱惩罚逻辑各执行 3 次 FFT (主信号 -> 参考信号覆盖峰值 -> 主信号重算)
- **修改后**: 调换执行顺序 -- 先做参考信号 FFT 取运动主频，再做主信号 FFT 取心率峰值，每路径仅需 2 次 FFT
- **位置**: `hr_algorithm.c` Step 8/9/10 频谱惩罚分支
- **效果**: 每秒减少 3 次 4096 点 FFT 计算

### ~~P1 - 中值平滑目标错误~~ ✅ 已完成 (2026-03-29)

- **修改前**: `DSP_MedianSmooth` 仅对 `hr_history_fft` 做平滑，但融合心率在运动时取 `hr_lms_hf`
- **修改后**: 中值平滑跟随融合路径: 运动段平滑 `hr_history_lms_hf`, 静息段平滑 `hr_history_fft`
- **位置**: `hr_algorithm.c` Step 12

### ~~P2 - sqrt 循环简化~~ ✅ 已完成 (2026-03-30)

- **修改前**: 手写 for 循环包含不必要的负数钳制 (`if (_v < 0.0f) _v = 0.0f`)
- **修改后**: 删除负数钳制 (输入恒为 ax^2+ay^2+az^2 >= 0)，`sqrtf()` 在 Cortex-M4F 上已编译为硬件 VSQRT 指令
- **位置**: `hr_algorithm.c` Step 5 运动检测

### P2 - ACC 原始数据包只发高 8 位

- **现状**: 21 字节原始数据包中 ACC 字段仅 3 字节 (X/Y/Z 各高字节)
- **位置**: `main.c` 数据打包逻辑
- **建议**: 算法内部使用完整 16-bit ACC，但原始数据包精度不足。如需离线分析，建议扩展协议或增加完整的 16-bit ACC 数据字段

### P3 - s_config 全局静态变量

- **现状**: 配置存在模块级静态变量中
- **位置**: `hr_algorithm.c:18`
- **建议**: 如果未来需要多实例（如同时监测多人），应将 config 指针存入 `HR_State_t` 结构体内。当前单实例无问题，优先级最低

---

## 三、中长期优化方向

### 降采样评估

125Hz 采样对于心率信号 (<5Hz) 而言偏高，Nyquist 只需 >10Hz。如果实测证明 25Hz 或 50Hz 就够用:

- FFT 从 4096 降到 1024/2048
- RAM 节省 16-24KB
- 算力减半
- 窗口长度不变，只是每秒处理的采样点减少

### 信号质量评估模块

MATLAB 参考代码 (`outline-HRsolver/PpgPeace.m`) 中有信号质量评估模块，在线版本尚未集成:

- 在信号质量差时抑制 HR 输出，避免误报
- 可作为融合决策的额外权重因子
- 预估额外 RAM 开销约 2KB

### 自适应 FFT 镇长度

- 静止时信号稳定，2048 点 FFT 足够 (0.061Hz 分辨率 = 3.7BPM)
- 运动时用 4096 点保持精度
- 动态切换可节省算力

### ~~上位机协议优化~~ ✅ 部分完成 (2026-03-28)

- **修改前**: 原始数据包 (125Hz) 和 HR 结果包 (1Hz) 同时发送，占用约 2.5KB/s 带宽
- **修改后**: 添加 `ENABLE_RAW_DATA_PACKET` 开关 (main.c)，默认 0 (不发送原始数据)
- **建议**: 后续可增加命令字，由上位机动态控制是否接收原始数据

---

## 四、调试验证优先级建议

建议按以下顺序进行硬件调试:

1. ~~烧录固件，确认传感器检测通过 (UART 调试信息)~~
2. 上位机连接，确认 HR 结果包正确接收（原始数据包已默认关闭，需调试时可开启 `ENABLE_RAW_DATA_PACKET = 1`）
3. 静息采集 60 秒，记录 CSV，验证 FFT 路径心率准确性
4. 行走采集 60 秒，记录 CSV，验证运动检测和 LMS-ACC 路径
5. 跑步采集 60 秒，记录 CSV，验证剧烈运动下的稳定性
6. ~~根据步骤 3-5 的数据，调整运动阈值和 BPF 参数~~ (已根据实测校准)
7. ~~**修复 LMS 系数重置问题** (P0 优先级，最大算法缺陷)~~ ✅ 已完成 (2026-03-29) - 改为条件性初始化

---

## 五、已完成修改记录

| 日期 | 项目 | 修改内容 |
|------|------|----------|
| 2026-03-28 | 运动阈值校准 | 添加 HR_ACC_LSB_PER_G 常量 (8192 LSB/g)，默认阈值改为 0.07g，内部自动转换 |
| 2026-03-28 | BPF 系数验证 | 扩展到 4 个 biquad 节，使用 MATLAB butter() 正确系数，验证通过 |
| 2026-03-28 | 配置开关集中 | 将 CURRENT_WORK_MODE 移至 main.c，添加 ENABLE_RAW_DATA_PACKET 开关 |
| 2026-03-29 | LMS 收敛性修复 | Step 7 改为条件性 LMS 初始化：仅在首次运行或阶数变化时重置，保留系数跨窗口连续自适应 |
| 2026-03-29 | 融合策略修正 | 运动段选择 LMS-HF 为主路径，中值平滑跟随融合路径 |
| 2026-03-29 | 数据包互斥模式 | ENABLE_RAW_DATA_PACKET=1 时完全跳过算法，仅发原始数据包 |
| 2026-03-30 | 重复FFT消除 | 调换 FFT 执行顺序 (先参考后主信号)，每秒减少 3 次 4096 点 FFT |
| 2026-03-30 | sqrt 循环简化 | 删除不必要的负数钳制 (输入恒 >= 0) |
