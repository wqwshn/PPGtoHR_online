# 在线心率算法实施文档

> 本文档记录在线心率估计算法从 MATLAB 离线版移植到 STM32L452CEU6 在线版的完整上下文，供后续开发和调试参考。

---

## 1. 项目概述

### 1.1 目标
将 MATLAB 离线心率解算算法 (PPG + LMS 自适应滤波 + FFT 融合) 部署到 STM32L452CEU6 单片机，实现 125Hz 采样、1Hz 心率输出的实时在线运行。

### 1.2 硬件平台
| 参数 | 值 |
|------|-----|
| MCU | STM32L452CEU6 |
| 内核 | ARM Cortex-M4F (带单精度 FPU) |
| 主频 | 80 MHz |
| RAM | 160 KB |
| Flash | 512 KB |
| 采样率 | 125 Hz (TIM16 定时器硬件固定) |
| 数据通道 | PPG (MAX30101 绿光) + ACC (MIMU LSM6DS3, SPI2) + HF/ADC (ADS124S06, SPI1, 4 通道桥臂) |
| 通信 | UART2 @ 115200 bps (蓝牙透传) |

### 1.3 传感器映射
| 信号 | 传感器 | 接口 | 数据格式 |
|------|--------|------|----------|
| PPG | MAX30101 (绿光 LED) | I2C (PA0/PA1) | 18-bit, 1000sps/4xavg = ~250 FIFO/s |
| ACC X/Y/Z | MIMU LSM6DS3TR-C | SPI2 (PB7/PB8 CS) | 16-bit signed, +/-4g |
| HF (桥臂) | ADS124S06 | SPI1 (PA4 NSS) | 24-bit signed, 4 通道 |
| 角速度 | LSM6DS3TR-C (陀螺) | SPI2 | 16-bit (算法未使用) |

### 1.4 当前工作模式
`CURRENT_WORK_MODE = MODE_HEART_RATE` (绿光 PPG 心率模式)

---

## 2. 算法架构

### 2.1 整体流水线

```
TIM16 ISR (125Hz)
    |
    v
START_CONV pulse --> ADC DRDY ISR
    |                       |
    v                       v
ADC 4通道读取          ACC_6BytesRead()
    |                       |
    +--------+--------+------+
             |
             v
    Main Loop: ADC_1to4Voltage_flag == 4
             |
             +-- PPG FIFO 批量读取 + 均值 --> float ppg_val
             +-- ACC 16-bit --> float ax, ay, az
             +-- ADC/HF 16-bit --> float hf_val
             |
             v
    HR_PushSample() --> buf_1s_*[125] (每 tick 1 个 float)
             |
             v (当 sample_idx >= 125)
    flag_1s_ready = 1
             |
             v
    HR_RunSolver() [每秒执行一次]
             |
    1. 原始窗口平移 (memmove + memcpy, 5 通道)
    2. IIR 流式滤波 (仅新 125 点, 状态连续)
    3. 滤波窗口平移
    4. 运动检测 (ACC 幅值 std vs 阈值)
    5. 窗口填充检查 (需 >= 8 秒)
    6. 时延对齐 (去均值点积, +/-5 采样点)
    7. 路径A: LMS-HF (2级级联) + FFT + 频谱惩罚
    8. 路径B: LMS-ACC (3级级联) + FFT + 频谱惩罚
    9. 路径C: 纯FFT + Hamming窗 + 频谱惩罚
   10. 融合: 运动→LMS-ACC, 静息→FFT
   11. 5点中值平滑 + 变化率限制
   12. 输出: hr_bpm (float)
             |
             v
    21字节心率结果包 --> UART DMA --> 蓝牙
    21字节原始数据包 --> UART DMA --> 蓝牙 (调试共存)
```

### 2.2 MATLAB 算法到 C 的关键映射

| MATLAB 函数 | C 实现 | CMSIS-DSP API |
|-------------|--------|---------------|
| `filtfilt` (零相位双向滤波) | 单向流式 IIR | `arm_biquad_cascade_df1_f32` |
| `fft(signal, 8192)` 复数 FFT | 4096 点实数 RFFT | `arm_rfft_fast_f32` |
| `corr(ppg, ref)` 皮尔逊相关 | 去均值点积近似 | `arm_dot_prod_f32` |
| 自定义 NLMS (`lmsFunc_h.m`) | 归一化 LMS | `arm_lms_norm_f32` |
| `findpeaks` | 手动局部极大值检测 | 无 |
| `sort(amps, 'descend')` | 插入排序 | 无 |
| `smoothdata(..., 'movmedian', 5)` | 环形缓冲区中值 | 无 |

### 2.3 三路径融合策略

```
            ┌─────────────────┐
            │  滤波后 PPG 窗口  │
            └────────┬────────┘
                     │
       ┌─────────────┼─────────────┐
       v             v             v
  ┌─────────┐  ┌──────────┐  ┌──────────┐
  │ 路径A   │  │ 路径B    │  │ 路径C    │
  │ LMS-HF  │  │ LMS-ACC  │  │ 纯 FFT   │
  │ 2级级联  │  │ 3级级联   │  │ Hamming  │
  │ HF参考   │  │ ACC参考   │  │ 窗       │
  └────┬────┘  └────┬─────┘  └────┬─────┘
       │            │             │
       v            v             v
  hr_lms_hf    hr_lms_acc     hr_fft
       │            │             │
       └────────────┼─────────────┘
                    │
              ┌─────┴──────┐
              │ 运动检测    │
              │ is_motion?  │
              └─────┬──────┘
                    │
         ┌──────────┴──────────┐
         v                     v
    运动=hr_lms_acc       静息=hr_fft
         │                     │
         └──────────┬──────────┘
                    v
              hr_fused
                    v
           5点中值+变化率限制
                    v
              hr_bpm (输出)
```

---

## 3. 设计决策记录

### 3.1 保留全部 3 条路径
- **讨论**: 是否只保留 ACC 路径以节省计算量？
- **结论**: 保留全部 3 条。预估算力 ~9ms/秒 (仅 1% CPU), 实时性无压力。调试阶段需要对比各路径结果。
- **文件**: 已在算法初始化中分配了 HF (2级) 和 ACC (3级) 的完整 LMS 状态。

### 3.2 运动阈值: 混合方案
- **讨论**: 每次自动标定 vs 固定阈值？
- **结论**: 采用混合方案:
  1. 开机使用硬编码默认值 `Default_Motion_Th = 200.0f` (立即可用)
  2. 后台自动标定: 收集 8 个静息窗口的 ACC std 均值, 计算 `Motion_Th_Scale * baseline_std` 覆盖默认值
  3. 标定期间不阻塞心率输出, 使用默认阈值判断运动
- **风险**: 默认值需要根据实际硬件调试调整。不同佩戴方式可能影响基线。

### 3.3 蓝牙输出 1Hz 心率包
- **讨论**: 继续发送 125Hz 原始数据还是只发心率？
- **结论**: 两种共存:
  - 原始数据包: 21 字节 x 125Hz = 2.6 KB/s (保留用于调试)
  - 心率结果包: 20 字节 x 1Hz (新增, 主输出)
  - 心率包包含: 融合BPM/运动标志/3路径BPM/PPG信号强度/校准状态/时间戳/XOR校验

### 3.4 IIR 滤波: 流式策略
- **关键**: 不能对 8 秒窗口重新滤波 (IIR 是因果滤波器)
- **方案**: 仅对新增 125 个样本滤波, 状态在 biquad 实例中自然传递
- **代价**: 需要维护独立的 `filt_*` 滤波后窗口 (额外 20KB RAM)

### 3.5 FFT 精度权衡
- **MATLAB**: 8192 点复数 FFT (分辨率 0.015 Hz = 0.9 BPM)
- **C**: 4096 点实数 RFFT (分辨率 0.031 Hz = 1.8 BPM)
- **评估**: 1.8 BPM 分辨率对心率检测足够, 节省一半内存

---

## 4. 文件结构与模块说明

### 4.1 新增文件 (分支 feature/hr-solver-online)

| 文件 | 行数 | 职责 |
|------|------|------|
| `Core/Inc/hr_algorithm.h` | ~240 | 主 API、配置/状态结构体、编译常量 |
| `Core/Inc/hr_dsp.h` | ~120 | DSP 原语声明 (FFT/滤波/峰值/惩罚) |
| `Core/Inc/hr_lms.h` | ~80 | LMS 自适应滤波器声明 |
| `Core/Src/hr_algorithm.c` | ~753 | 算法编排: Init/PushSample/RunSolver/DefaultConfig |
| `Core/Src/hr_dsp.c` | ~313 | FFT峰值提取/时延搜索/排序/追踪/惩罚/中值 |
| `Core/Src/hr_lms.c` | ~120 | LMS_Init/Process/CascadeProcess |

### 4.2 修改文件

| 文件 | 修改内容 |
|------|----------|
| `CMakeLists.txt` | 启用 CMSIS-DSP (include/lib/macro) |
| `Core/Src/main.c` | 前后台架构集成、float 数据采集、算法调用、结果输出 |
| `Core/Inc/main.h` | 工作模式/数据包长度宏 (原有, 未修改) |

### 4.3 参考文件 (离线算法)

| 文件 | 用途 |
|------|------|
| `outline-HRsolver/HeartRateSolver_cas_chengfa.m` | 核心离线算法 (238行) |
| `outline-HRsolver/lmsFunc_h.m` | NLMS 自适应滤波器 |
| `outline-HRsolver/FFT_Peaks.m` | FFT 频谱峰值提取 |
| `outline-HRsolver/ChooseDelay1218.m` | 时延与相关系数计算 |
| `outline-HRsolver/Find_maxpeak.m` | 峰值按幅值排序 |
| `outline-HRsolver/Find_nearBiggest.m` | 心率历史追踪 |
| `outline-HRsolver/PpgPeace.m` | 信号质量评估 (在线版未使用) |
| `outline-HRsolver/离线算法部署为在线算法修改指南.md` | 移植指南文档 |

---

## 5. 内存预算

### 5.1 算法 RAM 占用 (~90 KB / 160 KB)

| 缓冲区 | 大小 | 说明 |
|--------|------|------|
| `win_ppg/accx/accy/accz/hf[1000]` | 20 KB | 8 秒原始滑动窗口 (5 通道) |
| `filt_ppg/accx/accy/accz/hf[1000]` | 20 KB | 8 秒滤波后滑动窗口 (5 通道) |
| `fft_input/output[4096]` | 32 KB | FFT 工作缓冲区 |
| `buf_1s_ppg/accx/accy/accz/hf[125]` | 2.5 KB | 1 秒采集缓冲区 |
| `iir_filt_1s_ppg/accx/accy/accz/hf[125]` | 2.5 KB | 1 秒 IIR 滤波输出 |
| `hamming_win[1000]` | 4 KB | Hamming 窗系数 (预计算) |
| `scratch_a/b[1000]` | 8 KB | zscore 归一化暂存 |
| `lms_hf/acc state + coeffs + tmp/tmp2` | ~6 KB | LMS 滤波器状态 |
| `hr_history[120] x 3` | ~1.4 KB | 心率历史 |
| 其他 (IIR state, peaks, config...) | ~1 KB | 零散状态 |
| **合计** | **~89 KB** | **剩余 ~51 KB** |

### 5.2 Flash 占用
- 算法代码 + CMSIS-DSP 库: ~26% Flash (140 KB / 512 KB)

---

## 6. 关键技术细节

### 6.1 IIR 带通滤波器
- **类型**: 4 阶 Butterworth (2 个 biquad 节)
- **频率**: 0.5 - 5 Hz (对应 30-300 BPM)
- **系数来源**: `scipy.signal.butter(4, [0.5, 5], btype='bandpass', fs=125, output='sos')`
- **CMSIS-DSP 格式**: `[b0, b1, b2, -a1, -a2]` 每节 5 个系数
- **流式要求**: 每秒仅滤波新 125 样本, biquad 状态跨调用保持

### 6.2 LMS 自适应滤波
- **CMSIS-DSP API**: `arm_lms_norm_f32`
- **参数映射**: pSrc=参考噪声(ACC/HF), pRef=期望信号(PPG), pErr=去噪输出
- **级联**: HF 路径 2 级, ACC 路径 3 级
- **双缓冲区**: 级联使用 tmp/tmp2 交替, 避免读写冲突
- **自适应步长**: `mu = LMS_Mu_Base - corr/100` (corr 为相关系数)
- **自适应阶数**: `order = min(Max_Order, max(1, floor(|delay| * factor)))`

### 6.3 时延搜索 (ChooseDelay)
- **范围**: d = -5 到 +5 采样点
- **方法**: 对 PPG 和参考信号去均值后计算点积 (近似皮尔逊相关)
- **简化**: 相比 MATLAB 的 `corr()`, 在线版用去均值点积替代, 减少计算量
- **ref 缓冲区偏移**: ref 数组从索引 5 开始对齐, ±5 偏移通过调整起始索引实现

### 6.4 zscore 归一化
- **位置**: LMS 调用前, 在 scratch_a/b 上操作 (不污染 filt_* 数据)
- **步骤**: arm_mean → arm_offset(减均值) → arm_std → arm_scale(除标准差)
- **保护**: std < 1e-6 时钳制, 防止除零

### 6.5 频谱惩罚
- **目的**: 抑制运动频率及其二次谐波对心率检测的干扰
- **方法**: 参考 (ACC/HF) 信号 FFT 主峰 = 运动频率; PPG 峰值中接近运动频率的幅值乘以惩罚权重
- **参数**: 惩罚宽度 0.2 Hz, 惩罚权重 0.2

---

## 7. 数据输出协议

### 7.1 原始数据包 (21 字节, 125Hz, 保留)
```
[0xAA][0xBB] [ADC0:2][ADC1:2][ADC2:2][ADC3:2] [ACC_H:3] [PPG:4] [扩展:2] [XOR:1] [0xCC]
```

### 7.2 心率结果包 (20 字节, 1Hz, 新增)
```
偏移  字段                    类型       说明
0-1   帧头                    uint8 x2   0xAA, 0xCC
2-3   融合心率 BPM            uint16     x10 精度 (72.5 BPM = 725)
4     运动标志                 uint8      0=静息, 1=运动
5     窗口填充状态             uint8      0=未满, 1=已满
6-7   LMS-HF 路径 BPM         uint16     x10 精度
8-9   LMS-ACC 路径 BPM        uint16     x10 精度
10-11 FFT 路径 BPM            uint16     x10 精度
12-13 PPG 信号均值             uint16     信号强度参考
14    运动校准状态             uint8      0=未校准, 1=已校准
15-16 时间戳                  uint16     秒计数器
17    校准窗口进度             uint8      0-8 (收集到几个静息窗口)
18    XOR 校验                 uint8      bytes[2..17] 异或
19    帧尾                    uint8      0xCC
```

---

## 8. 默认参数表

| 参数 | 默认值 | 说明 |
|------|--------|------|
| LMS_Mu_Base | 0.01 | LMS 基础步长 |
| Spec_Penalty_Width | 0.2 Hz | 频谱惩罚宽度 |
| Spec_Penalty_Weight | 0.2 | 频谱惩罚权重 (越小抑制越强) |
| HR_Range_Hz | 0.25 Hz | 运动段心率搜索范围 |
| HR_Range_Rest_Hz | 0.4 Hz | 静息段心率搜索范围 |
| Slew_Limit_BPM | 10.0 BPM/s | 运动段心率变化率上限 |
| Slew_Step_BPM | 2.0 BPM | 运动段心率单步步进 |
| Slew_Limit_Rest_BPM | 8.0 BPM/s | 静息段心率变化率上限 |
| Slew_Step_Rest_BPM | 1.0 BPM | 静息段心率单步步进 |
| Motion_Th_Scale | 3.0 | 运动阈值倍数 (x baseline_std) |
| Default_Motion_Th | 0.07 g | 默认运动阈值 (g 值，内部转换为 LSB) |
| Spec_Penalty_Enable | 1 | 启用频谱惩罚 |

**注意**: 运动阈值使用 g 值单位配置，内部通过 `HR_ACC_LSB_PER_G = 8192.0f` 转换为 LSB (LSM9DS1 @ ±4g 量程)。

---

## 9. 提交历史

```
8a3b2c1 feat: 添加采样率字段到心率结果包 (2026-03-28)
        - HR结果包从20字节扩展到21字节
        - 新增字节18: 采样率 (固定125Hz)
        - 更新上位机协议解析, 支持采样率显示
        - CSV导出新增sampling_rate列
796c450 fix: 修复运动阈值和BPF系数 (2026-03-28)
        - 添加 HR_ACC_LSB_PER_G 常量 (8192 LSB/g for LSM9DS1 @ ±4g)
        - 运动阈值改为 g 值单位，默认 0.07g (基于实测静息 0.06-0.07g)
        - BPF 扩展到 4 个 biquad 节，使用 MATLAB butter() 正确系数
        - 验证通过: -3dB 截止 0.5/5.0Hz，通带衰减 4.35dB
        - 添加 ENABLE_RAW_DATA_PACKET 开关，默认关闭原始数据包
        - 配置开关 (CURRENT_WORK_MODE) 移至 main.c 顶部
750624b fix: resolve compilation issues and update BPF coefficients
4e82cc6 feat: add HR result debug output protocol
5a0efac feat: integrate online HR algorithm into main loop
f6941d6 feat: implement HR algorithm orchestrator with 3-path fusion
f147363 feat: implement cascaded NLMS adaptive filter wrapper
b6f32bc feat: implement DSP primitives (FFT peaks, delay, spectrum penalty)
a73ab2d feat: add algorithm header files with API definitions
4f3f1d0 feat: enable CMSIS-DSP library for Cortex-M4F
```

---

## 10. 已知待办 / 调试要点

### 10.1 需要实机验证的项目
1. ~~**BPF 系数验证**~~ ✅ 已完成 (2026-03-28) - 扩展到 4 个 biquad 节，MATLAB 验证通过
2. ~~**运动阈值 Default_Motion_Th = 200.0**~~ ✅ 已完成 (2026-03-28) - 改为 g 值单位，默认 0.07g
3. **PPG 均值合理性**: MAX30101 绿光 18-bit 数据均值范围约 50000-200000, 需确认 zscore 归一化前信号质量
4. **LMS 收敛性**: 首次运行时 LMS 系数全零, 前几秒输出可能不稳定。**注意**: 每秒重置系数是已知问题 (见 10.3)
5. **FFT 分辨率**: 4096 点 vs MATLAB 的 8192 点, 需验证峰值检测是否受影响

### 10.2 潜在优化方向
1. **降采样**: 如果 125Hz 分辨率对心率检测过高, 可降采样到 25-50Hz 以减少计算量
2. **裁剪路径**: 确认 HF 路径价值后可裁剪, 节省 ~30% 计算量和 ~3KB RAM
3. **PpgPeace 信号质量评估**: 当前未集成, 可作为额外质量门控
4. **自适应 FFT 长度**: 静息时可用 2048 点, 运动时用 4096 点

### 10.3 注意事项
1. `filt_*` 缓冲区需要首尾各多 5 个采样点以支持 DSP_FindDelay 的 +/-5 偏移。当前实现中 ChooseDelay 直接在 filt_* 上操作, 偏移靠调整起始索引实现, 但存在边界风险。如果 8 秒窗口首尾数据不完整, 可能需要额外处理。
2. main.c 中 ACC 高位数据 `allData[10..12]` 仍然发送 (向后兼容), 但算法使用完整的 16-bit ACC。两者数据格式不同, 上位机需区分。
3. **P0 - LMS 每秒重置系数**: `LMS_Init` 在 Step 7 每秒调用, 清零所有系数。这意味着 LMS 滤波器永远无法充分收敛。**这是最大算法缺陷**, 需修复为仅在延迟变化时重置系数。

---

## 11. CMSIS-DSP API 参考

| 函数 | 用途 | 关键参数 |
|------|------|----------|
| `arm_biquad_cascade_df1_f32` | IIR 滤波 | inst, pSrc, pDst, blockSize |
| `arm_rfft_fast_f32` | 实数 FFT | inst, pSrc, pDst, ifftFlag |
| `arm_rfft_fast_init_f32` | 初始化 FFT | inst, fftLen |
| `arm_cmplx_mag_f32` | 复数幅度 | pSrc, pDst, numSamples |
| `arm_lms_norm_f32` | 归一化 LMS | S, pSrc, pRef, pOut, pErr, blockSize |
| `arm_lms_norm_init_f32` | 初始化 LMS | S, numTaps, pCoeffs, pState, mu, blockSize |
| `arm_dot_prod_f32` | 向量点积 | pSrcA, pSrcB, blockSize, *result |
| `arm_mean_f32` | 均值 | pSrc, blockSize, *result |
| `arm_std_f32` | 标准差 | pSrc, blockSize, *result |
| `arm_offset_f32` | 加偏移 | pSrc, offset, pDst, blockSize |
| `arm_scale_f32` | 乘标量 | pSrc, scale, pDst, blockSize |
| `arm_mult_f32` | 逐元素乘 | pSrcA, pSrcB, pDst, blockSize |

---

**最后更新**: 2026-03-28
**分支**: feature/hr-solver-online
**编译状态**: 通过 (arm-none-eabi-gcc 15.2.1, RAM 85%, Flash 27%)
